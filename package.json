{
  "name": "family-tree",
  "private": true,
  "scripts": {
    "start": "node server.cjs",
    "test": "node --test tests/*.test.cjs",
    "test:browser": "python tests/family-ui.py && python tests/ux-form-query.py && python tests/ux-canvas-dismiss.py && python tests/ux-member-list-toolbar.py",
    "build": "node build.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
