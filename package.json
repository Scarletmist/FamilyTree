{
  "name": "family-tree",
  "private": true,
  "scripts": {
    "start": "node server.cjs",
    "test": "node --test tests/*.test.cjs",
    "test:browser": "python tests/family-ui.py"
  },
  "engines": {
    "node": ">=20"
  }
}
